const BAD_REQUEST = 400;
const NOT_FOUND = 404;
const UNAUTHORIZED = 401;
const CONFLICTING_REQUEST = 409;
const FORBIDDEN = 403;
module.exports = {
  // eslint-disable-next-line max-len
  BAD_REQUEST, NOT_FOUND, CONFLICTING_REQUEST, UNAUTHORIZED, FORBIDDEN,
};